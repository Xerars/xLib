# HISTORY

#### [2026/03/26]
1. 建立 `xType` 庫 `(1.0.0)`
2. 建立 `xMeta` 庫 `(1.0.0)`
3. 建立 `Readme.md`  空白文件
4. 建立 `Rule.md`    空白文件
5. 建立 `Version.md` 空白文件
6. 建立 `.gitignore` 空白文件
7. 建立 `License.md` 空白文件


#### [2026/03/27]
1. `xType`庫
   1. [1.0.1]增加裝飾前綴的定義(`volatile,extern ...`)
   1. [1.0.2]增加NULL的定義
2. `xMeta`庫
   1. [1.0.1]E -> EXP 防止誤用
   2. [1.0.2]增加 `IS_ALIGN` 宏
   3. [1.0.3]增加 `SAFE_SWAP` 宏與 `SAFE_SORT` 宏
   4. [1.0.4]增加 `LIKELY` 宏與 `UNLIKELY` 宏
   5. [1.0.5]增加 `Coroutine` 相關宏
   6. [1.0.6]增加 `CAST` 宏
   7. [1.0.7]增加 `EXECUTE`宏
3. 完成第一版`.gitignore`


#### [2026/03/31]
1. 建立`xDLoad`庫`(1.0.0)`
   1. 建立函數庫
   2. 建立基本範例
   3. 建立工具`(CLI)`

#### [2026/04/05]
1. [1.0.1]修正`xDLoad` 標題說明
2. 建立`xDuff`庫`(1.0.0)`
   1. 建立函數庫
   2. 建立基本範例

#### [2026/04/06]
1. 建立`xVector`庫`(1.0.4)`
   1. 建立函數庫
   2. 建立基本範例
2. 範例測試加入 `Valgrind` 測試
3. 修正`xDLoad` 範例載入動態庫位址

#### [2026/04/22]
1. 建立`xTCA9555`庫`(1.0.0)`
   1. 建立函數庫
   2. 建立基本範例
2. 範例測試副檔名改名 `valgrid` -> `vgd`

#### [2026/04/25]
1. 建立`xCPrint`庫`(1.0.0)`
   1. 建立函數庫
   2. 建立基本範例
2. 修正`xType`庫對`DWORD`、`CCHAR`在`Windows`發生錯誤問題
3. 建立`xHexGen`應用程序`(1.0.6)`
4. 建立`xLeackCheck`庫`(1.1.1)`
   1. 建立函數庫
   2. 建立基本範例

#### [2026/05/15]
1. 建立`xCheckSum`庫`(1.0.0)`
   1. 建立函數庫
      1. `xCheckSum`庫
      2. `xCheckSum_BCC`庫
      3. `xCheckSum_LRC`庫
      4. `xCheckSum_CRC`庫
   2. 建立基本範例

#### [2026/05/17]
1. 建立`xTMP102`庫`(1.0.0)`
   1. 建立函數庫
   2. 建立基本範例

#### [2026/09/19]
1. 建立 `xSim` 模組 `(1.0.0)`
   1. 建立 `ADC`、`CLI`、`GPIO`、`INT`、`IPC`、`PWM`、`Reg`、`Timer` 與 `WDT` 周邊模擬 PlugIn
   2. 建立範例程式、日誌資料與 GUI 監控工具
   3. 建立中英文說明文件
2. 建立 `xSTM` 裝置工具庫 `(1.0.0)`
   1. 建立 `Debug`、`DWT`、`Flash`、`Sys` 與 `Power` PlugIn
   2. 建立 `STM32CubeMX`／`Keil MDK-ARM` 整合範例與中英文說明文件
   3. 新增目標 `ELF`／`AXF` 的函式庫大小量測說明
3. 更新 `xType` 至 `(1.0.6)`
   1. 擴充字元與字串型別定義
   2. 整理型別註解與巨集格式
4. 更新 `xMeta` 至 `(1.0.10)`
   1. 將通用運算子巨集改為 `_AND`、`_OR`、`_NOT` 等名稱，避免與 STM32 HAL 欄位衝突
   2. 新增位元操作、飽和加減與程式結束巨集
5. 更新 `xCPrint` 至 `(1.2.0)`
   1. 新增 `Shell` 與 `Lua` 使用支援及範例
   2. 補齊中英文說明文件
6. 整理 `xDevice` 目錄並移除舊版 `xTCA9555` 實作
